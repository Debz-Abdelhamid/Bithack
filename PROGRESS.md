# PROGRESS.md — Build state & decision log

> **For any AI agent/developer resuming work:** read `Claude.md`, `Schema.md`, `ui-design.md`,
> `Phases.md`, `Security.md` first (in that order), then this file. Update this file at the end
> of every phase. Never start a phase before the previous one's Definition of Done + Security
> gate passed and the project owner said go.

**Repo:** https://github.com/Debz-Abdelhamid/patrimo (private, branch `main`, CI must stay green)
**Last update:** 2026-07-08 — Phases 0–7 complete (incl. Phase 5 addendum + design-fidelity follow-up) · 162 tests green · next: **Phase 8 (Ticket closure, cost, PV de réception / PAdES)** — waiting for owner go

---

## Where we are

| Phase | Status | Highlights |
|---|---|---|
| 0 — Bootstrap | ✅ | Docker stack (nginx :8080 → php-fpm 8.4 non-root, PostgreSQL 16, Redis 7, queue, reverb, mailpit :8025, daily backup sidecar). Filament v4 panel themed from the legacy `Patrimo-BitHack` extraction (teal `#004c4c`/`#0f766e`, Plus Jakarta Sans, squared radii, UBMA brand). Legacy app archived in `legacy/` (own git history, git-ignored). QA: Pest + Larastan L6 + Pint; CI runs everything twice (sqlite + PostgreSQL). |
| 1 — Identity & RBAC | ✅ | Locked six roles + technical `super_admin` as spatie/shield **data** (zero-permission default, gate-based super admin, Shield `panel_user` disabled). TOTP MFA **enforced for A3/N2/N3/admin** (request-time middleware), 30-min elevated idle timeout, login lockout, Redis panel throttle (120/min). Realtime notifications via **Laravel Reverb** (ws://localhost:8081, private per-user channels, throttled `/broadcasting/auth`, queued). Domain-restricted **self-registration** (`PATRIMO_REGISTRATION_DOMAINS`, default univ-annaba.dz) + mandatory email verification (Mailpit locally) + hourly IP cap; password reset; email-change verification; institutional-domain rule on **all four** email surfaces (register, profile, admin create/edit — unchanged emails grandfathered). Account deactivation (`is_active`). Audit log: auth events (`login`/`logout`/`login_failed`/`password_reset`) + RBAC changes (role/permission attach/detach + Role lifecycle) + model changes (users/faculties/services). |
| 2 — Buildings & Rooms + map | ✅ | `buildings` (+`faculty_id` — documented Schema.md §2.1 divergence; NULL = central/shared) & `locals` with enums/factories/resources (Building has a Rooms relation manager). **FacultyScope** global scope: N2 sees own faculty + shared only (list, search, direct URL → 404); A3/N3 unscoped; `ViewAcrossFaculties` escape hatch exists ungranted. **Campus map ported as-is**: `maplibre-gl` v5 vanilla (React wrapper dropped), OpenFreeMap bright tiles, UBMA center zoom 17 / pitch 45, same SVG flag markers/tooltips/selection, rooms side panel, crosshair pick-a-location **through the Building update policy**. `PermissionSeeder` = matrix baseline (A3 full CRUD, N2/N3 read-only, map viewable by all roles). |
| 3 — Equipments + QR | ✅ | `equipments` (§2.3, `sub_category` relaxed nullable — documented) + `qr_codes` (**opaque UUID token**, unique morph pair) + `purchase_references` R7 stub. `EquipmentObserver`: auto inventory code `UBMA-YYYY-NNNNN` (manual entry kept; unique index = hard guarantee) + QR row on create, cleanup on delete. Equipment resource (filters, view page w/ 256px QR SVG block via simple-qrcode). **Print label**: A3-only (`PrintLabel:Equipment`), new tab → A4 → auto print, marks printed + audit (documented GET side effect). **Public lookup `GET /report/{token}`** — same URL contract as legacy so printed labels survive Phase 6; data-minimized card (no value/serial/notes/photo); UUID route constraint; Redis `qr-lookup` limiter (30/min/IP + 10/min/token+IP) verified live. FacultyScope → Equipment via room→building (unplaced = central, visible). N2/N3 read-only, tout_utilisateur denied. |

| 4 — Affectations | ✅ | `assignments` (§2.6): subject = equipment / whole room / equipment→room (permissive §1 reading, `TODO(confirm)` in Schema.md); targets service/person; `assigned_by` from session only; pg CHECK on subject. **AssignmentObserver**: new active assignment auto-closes the previous one (audit-logged, never deleted) + destination room syncs `equipments.local_id` (DoD: "updates current location/service, preserves history"). Resource + **Revoke** action (end today, policy-checked); history relation managers on Equipment (create w/ auto-bound subject, old "Affect" flow) + Room (read-only); current assignment on the Equipment infolist. RBAC: A3 full; **N2 create/update own faculty** (scoped-exists rules beat forged ids); delete A3-only; N3 read. |
| 5 — Room reservations | ✅ | `room_reservations` (§2.7) with a **`source` split** (2026-07-06 reversal, see decision log): `timetable` (N2/A3-authored, created directly `confirmed`, teacher picked from a dropdown) vs `request` (Enseignant ad-hoc, starts `pending`). Pg **EXCLUDE USING gist** (btree_gist, partial `WHERE status='confirmed'`) verified rejecting a raw overlapping insert + `RoomReservationObserver` app-level guard on every driver (defense in depth, matches Phase 4's CHECK-constraint pattern). **`ReservationApprovalService`**: confirm (re-checks overlap, auto-rejects other overlapping pending requests — open question #5 default) / reject (optional reason) / notifyApprover (room's-faculty N2, or A3 for shared rooms). `RequestReservation` (Enseignant-only ad-hoc form + own-requests cancel, campus-wide catalog, per-user hourly rate limit) and `ReservationAvailability` (read-only weekly grid, every role). Queued `SendReservationNotification` job (request rows only, Security.md §7 payload-minimized). **Bug found & fixed during testing**: business logic dereferencing `$reservation->local->building` silently returned null when the acting user's own faculty differed from the room's (Local/Building's own FacultyScope is keyed to *whoever is authenticated*, not the reservation's owner) — fixed with explicit `withoutGlobalScope` reads, both eager-load levels (local *and* nested building). |
| 5 addendum — Departments/Terms/Grid | ✅ | User-caught pagination bug fixed first: `RequestReservation`'s room picker was eagerly loading every row via `->options()` — switched to Filament's lazy `getSearchResultsUsing()`/`getOptionLabelUsing()` (bounded, per-keystroke query); confirmed every other `->relationship()` select in the app already used `->searchable()` (Filament's built-in lazy AJAX search) and all tables paginate by default. Then, per owner decision: **`departments`** (belongs to a faculty, FacultyScope'd — N2 own faculty only, no shared/central branch) and **`academic_terms`** (year = 2 semesters, university-wide referential, A3-managed) promote `room_reservations.department` from free text to `department_id` FK + new `academic_term_id` FK; recurrence now runs to the term's `end_date` (the old arbitrary "repeat until" + month-cap config is gone) via a shared `TimetableSlotService` (extracted from `CreateRoomReservation`, reused by the new grid). **`TimetableBuilder`** page — the actual visual weekly grid the legacy app had (ported exactly: 6 fixed periods × Sat–Thu columns, confirmed from the legacy `data.ts` seed source, correcting an earlier extraction note that wrongly suspected Mon–Sat) — N2 (own faculty's departments)/A3 pick a department + term and fill the grid directly; cancelling a card cancels the whole recurring series. Gotcha hit: Filament's `->relationship()` Select assumes a saveable bound record and threw on this record-less page — fixed by reusing the same lazy-search pattern as `RequestReservation`. **Design-fidelity follow-up (2026-07-06)**: user caught that the first pass only ported the grid's *mechanics*, not the legacy's actual layout — closed with a 3-column layout (left "Academic Structure" Faculty→Department tree, click-to-select), richer card anatomy (Heroicon lines, status badge, hover-reveal cancel), and a status legend footer (see `ui-design.md` §5 for what was deliberately *not* reproduced and why). Caught mid-fix: the new Tailwind utility classes didn't render until `npm run build` re-ran (stale `public/build` manifest) — a build-step gotcha worth remembering for any future blade-only visual change. |
| 6 — Anomaly reporting → ticket | ✅ | `maintenance_tickets` (§2.8, `reference` column added, `category` relaxed nullable — both documented divergences) grows the Phase 3 public lookup page rather than a new surface: `GET /report/{token}` (`QrLookupController`) stays public/unauthenticated (Phase 3's shipped contract, deliberately not re-gated); only the new `POST /report/{token}` (`AnomalyReportController`) requires a session, with its own Redis limiter (`anomaly-report`, per-user + per-token). **Priority is always `urgent`/48h for a QR-scan report** — matched directly from the legacy `ReportView.tsx` (no category picker ever existed there), rather than inventing the category→priority mapping `Phases.md`/this file's earlier draft implied but never specified (flagged below, not guessed). `MaintenanceTicketObserver` computes `sla_due_at` at creation (`TicketPriority::slaDueAt()` — `+48h` urgent, `+5 business days` standard skipping Friday only; full holiday calendar stays open question #1), assigns a `TCK-YYYY-NNNNN` reference (same pattern as `EquipmentObserver`), and auto-routes `assigned_service_id` from the equipment's current active assignment when left blank. Legacy-matched duplicate-report guard (`MaintenanceTicket::hasActiveTicketFor()`) shows "already reported" instead of a second form. Queued `SendTicketNotification` to A3 + the routed service's `responsible_user` (ids/labels only). `MaintenanceTicketResource` — A3 full CRUD, N2/N3 read (N2 FacultyScope'd via equipment/local→building, following the general enforcement rule rather than an explicit matrix line — flagged), Service technique read-only unscoped (no per-technician sub-scoping exists, matching the Phase 7 `interventions.technician_id` note already in Schema.md §2.9), Enseignant/tout_utilisateur Create-only (report, no browsing) per the matrix. 18 new Pest tests (147 total). |
| 7 — Ticket workflow & SLA engine | ✅ | **Kanban plugin evaluated, not used**: `mokhosh/filament-kanban` requires Filament `^3.0` (no official v4 support; only an unofficial third-party fork claims it) — built a custom `MaintenanceBoard` Filament Page instead, native HTML5 drag-and-drop (Alpine `x-on:dragstart/dragover/drop`), matching the legacy `TicketsBoardView.tsx` exactly (it also turned out to be plain native drag-and-drop, no client library). `TicketStatus` gets a real state machine (`canTransitionTo()`/`next()`) — linear `new → assigned → in_progress → resolved → closed` (matches both Phases.md's column order and the legacy `TicketDetailView.tsx`'s own `NEXT_STATUS` map), plus `cancelled` as a side branch from any non-terminal status. **One enforcement point** (`TicketWorkflowService`) is the only place a ticket's status is ever written — the Kanban drag and the ticket page's "Advance status"/"Cancel ticket" actions both call it, so neither can bypass the state machine; the plain Edit form's `status` field is disabled post-creation for the same reason. `interventions` (§2.9) + `InterventionsRelationManager` on the ticket resource — A3 assigns technician/schedule (full CRUD), the assigned Service technique member later logs their own report/cost/completion via a custom `logWork` ability (`InterventionPolicy` compares `technician_id`, never a role-name check). `patrimo:escalate-tickets` scheduled job (new `scheduler` container running `schedule:work`, every 15 min) notifies A3 + the routed N2 once per ticket (`escalated_at` idempotency guard) when a ticket is ≥80% through its SLA window or past it — the "80% elapsed" check runs in PHP, not SQL, since `sla_due_at - created_at` interval arithmetic isn't portable between Postgres and the sqlite test driver. 15 new Pest tests. |

**Tests:** 162 green (sqlite + pgsql in CI). **English-first UI** (owner decision; `lang/en` primary, `lang/fr` maintained). **Timezone:** Africa/Algiers.

## Decision log (owner decisions, dated — do not re-litigate)

- **2026-07-04** Realtime = **Reverb** (self-hosted, Pusher protocol), notifications only, minimal payloads.
- **2026-07-04** **Booking initiation = Enseignant only**; `tout_utilisateur` = read-only timetable + QR reporting. N2 approves, A3 administers. *(Superseded 2026-07-06 for recurring slots — see below; ad-hoc requests still Enseignant-only.)*
- **2026-07-04** Self-registration: institutional domains only, verified, throttled, auto `tout_utilisateur`. Provisioned accounts pre-verified.
- **2026-07-05** English-first UI (reversal of French-first; strings stay in lang files). Timezone Africa/Algiers.
- **2026-07-05** `faculty_id` semantics: **N2 = required authorization boundary** (form-enforced); teacher/user = affiliation only, never filters rooms; empty = central. **Approval routes to the ROOM's faculty N2**, not the requester's.
- **2026-07-05** Phase 5 form fields: requester read-only from account; module + level required for course bookings; department free-text until R9; attendees ≤ room capacity (Schema.md §2.7).
- **2026-07-06** Emploi du temps = confirmed recurring reservations (no separate timetable entity — the academic referential is R9's, ui-design.md §9.4).
- **2026-07-06 (superseding the entry above)** Owner corrected the direction: the timetable is **faculty-authored, not teacher-authored**. `room_reservations` gets a `source` enum (`timetable`/`request`, Schema.md §2.7): **N2 enters/edits their own faculty's recurring course slots directly as `confirmed`** (teacher picked from a dropdown, never typed); **A3 does the same for central/shared rooms** (gap closed: shared rooms have no N2 to own them). **Enseignant keeps ad-hoc/one-off `request` bookings** (makeup classes, defenses, meetings) in whatever gaps remain — still `pending` until the room's-faculty N2 (or A3 for shared rooms) approves. This reverses the 2026-07-04 "recurring slots initiated by teachers" wording while keeping teachers as the sole ad-hoc requesters. Reflected in `Security.md` §3, `Schema.md` §2.7, `Phases.md` Phase 5, `ui-design.md` §5.
- **2026-07-06 (Phase 5 addendum)** Owner asked for the actual visual timetable grid (not just a plain form) and clarified the real structure: **the academic year is 2 semesters; a faculty manages several departments and fills each department's timetable one semester at a time.** Confirmed: (1) `departments` becomes a real entity (belongs to a faculty), replacing free-text `department`; (2) `academic_terms` becomes a real entity (year + semester + date range), timetable recurrence now runs to the term's `end_date` instead of an arbitrary picked date; (3) N2/A3 fill the timetable through a **visual weekly grid** (`TimetableBuilder`), ported from the legacy app's exact layout, not the plain create-form alone. Schema.md §2.7/§2.14/§2.15, Phases.md Phase 5 addendum, ui-design.md §5/§9.5 updated.
- **2026-07-06 (design-fidelity follow-up, flagged not guessed)** Closing the visual gap between `TimetableBuilder` and the legacy `ReservationsView.tsx` required a scoping call: the legacy's left sidebar ("Academic Structure") is a full inline CRUD panel for faculties/departments/**specialities/class groups/levels**. Only `departments` and `academic_terms` were approved as real R13 entities (see the decision above) — specialities/groups/class-levels were never asked for and don't exist in R13's schema. Rather than silently inventing those entities to fully replicate the sidebar, the port keeps the sidebar to what R13 actually models: a **read-only Faculty→Department navigation tree** (click to select, backed by the real `departments`/`faculties` tables). `TODO(confirm)`: if the university wants specialities/class-groups managed as real data (not just the existing free-text `level`/`student_group` fields on `room_reservations`), that is R9 academic-referential scope and should be scoped as its own addendum, not folded into R13 silently.
- **2026-07-07 (Phase 6, flagged not guessed)** `Schema.md`/`Phases.md` describe `maintenance_tickets.priority` as "defaulted per category rules" but never actually specify the mapping. Direct inspection of the legacy report-submission component (`ReportView.tsx`) shows it never implemented this either — every QR-scan report is hardcoded urgent/48h, with no category field on the form at all. Matched the legacy exactly rather than inventing a mapping: `source = qr_scan` tickets are always `priority = urgent`; `category` stays a plain classification/routing field. `TODO(confirm)`: revisit if the university actually wants graduated severity by category — that would be a real policy input, not something to guess at.
- **2026-07-07 (Phase 6, consistency default, flagged)** Security.md's role matrix doesn't spell out ticket *visibility* for N2/N3/Service technique explicitly (only A3's "full CRUD... ticket oversight" and Service technique's "plans and carries out interventions" are named). Extended the same read-only + FacultyScope pattern already applied to every other resource (N2 scoped, N3 unscoped) rather than leaving the new resource ungoverned; Service technique got broad unscoped read (no per-technician sub-scoping exists anywhere in the schema — Schema.md §2.9 already notes `interventions.technician_id` has no dedicated scoping mechanism either).
- **2026-07-08 (Phase 7, plugin evaluated per Phases.md's own instruction)** `mokhosh/filament-kanban` requires Filament `^3.0` — no official v4 support, only an unofficial third-party fork claims it. Built a custom `MaintenanceBoard` page instead of depending on an unofficial fork for a security-sensitive university system; the legacy board itself turned out to be plain native HTML5 drag-and-drop with no client library either, so nothing of substance was actually given up by going custom.
- **2026-07-08 (Phase 7)** New `scheduler` Docker service (`php artisan schedule:work`) added — this is the first scheduled job in the project (SLA escalation, `routes/console.php`). Any future scheduled work registers in the same place; no second scheduler container needed.

## Open questions (`TODO(confirm)` — never guess)

1. SLA business-day calendar (holidays/weekends) for "standard ≤ 5j" (Schema.md §6).
2. Monetary threshold triggering N3 approval + PAdES signature (Phases 8/10).
3. Definitive registration domain list (student subdomains?) — `PATRIMO_REGISTRATION_DOMAINS`.
4. ~~Department field → FK when R9 academic referential exists.~~ **Resolved 2026-07-06** — `departments` is now a real R13-owned entity (Schema.md §2.14), ahead of R9, per owner decision (Phase 5 addendum). `student_group`/`level` remain simple fields; a fuller academic referential (specialities, class groups, course catalog — as seen in the legacy app) is still out of R13 scope unless asked for.
5. Phase 5 defaults — **implemented, not yet confirmed with the university**: confirming a pending `request` auto-rejects other overlapping pending requests for the same room/time (notified as "rejected — room taken"). *(The recurrence-cap default is now moot — 2026-07-06 addendum replaced the arbitrary month cap with the Academic Term's `end_date` as the authoritative boundary.)*
6. Affectation semantics (Phase 4, Schema.md §2.6): an equipment assignment with a destination room also moves the asset (`equipments.local_id` synced) — permissive reading of §1; confirm whether affectation and physical relocation should be strictly separate operations instead.
7. Ticket `category` → `priority` mapping (Phase 6): `Schema.md`/`Phases.md` describe this but never specify it, and the legacy app never implemented it either — every QR-scan report is hardcoded urgent/48h for now (Schema.md §2.8 Phase 6 note). Confirm whether the university actually wants graduated severity by category before building a mapping.
8. Ticket visibility for N2/N3/Service technique (Phase 6): Security.md's role matrix doesn't spell this out explicitly — extended the same read-only + FacultyScope convention used everywhere else rather than leaving it ungoverned (PROGRESS.md decision log, 2026-07-07). Confirm this matches actual intent, particularly whether Service technique's unscoped read is too broad once real technician sub-teams exist.

## Operational notes

- Demo accounts: `admin@ / a3@ / n2@ / n3@ / technique@ / enseignant@ / utilisateur@ demo.ubma.dz`, password `password`. Elevated roles forced into MFA setup on login.
- Everything runs in Docker only — never `php artisan serve` on the host. App: http://localhost:8080/admin · Mail: :8025 · WS: :8081.
- Composer/npm/artisan run **inside** the container (`docker compose exec app …`); vendor/node_modules live in named volumes.
- After any `.env` change: `docker compose restart queue reverb`.
- **Never `migrate:fresh` the live dev DB** without asking — use a scratch database for fresh-seed gates.
- Gates before every push: `pint --test`, `phpstan analyse --memory-limit=1G`, `pest` — then push and watch CI (`gh run watch`).
- Per phase: end with a report (built / DoD mapping / Security.md §12 checklist), update CHANGELOG.md + this file, wait for owner's go.

## Next

**Phase 8 — Ticket closure, cost, PV de réception (PAdES)** (Phases.md): a closure form
(report, cost, restores `equipments.status = in_service`) — Closed is already a real
`TicketStatus` in the Phase 7 state machine, this phase adds what must happen *alongside*
reaching it. `reception_reports` table (Schema.md §2.10); PAdES signing flow for N2
(standard) / N3 (grands travaux) — **blocked on open question #2** (the monetary threshold
above which N3 + PAdES becomes mandatory is not given anywhere in the source docs; do not
guess a figure). Realtime notification to the responsible N2/N3 signer when a closure awaits
their signature, ids/labels only, no amounts in the broadcast payload. **Waiting for owner go
— and the threshold question should probably be resolved with the university before or
during this phase, not after.**
