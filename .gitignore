*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Archived legacy app (has its own .git — kept out of this repo's history)
/legacy

# Per-developer compose tweaks (e.g. publishing Postgres on 127.0.0.1 for GUI clients)
docker-compose.override.yml

# Database dumps (backup sidecar)
/storage/backups
